//not even zero because they both don't have a value
//undefined

let coffeCupContents;

console.log(coffee.CupContents);

//null (not placed in quotes)
//different from undefined because it null was made to be nothing
let userData = "username and email";
userData = null;
console.log("User Data:", userData);
